import { FC } from 'react';
import { IDefaultProps } from '../../types/Components.types';
import { ChartsWrapper } from './Index.styles';

const Charts: FC<IDefaultProps> = () => {
    return (
        <ChartsWrapper>
            Charts
        </ChartsWrapper>
    )
}

export { Charts };